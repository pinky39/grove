<UserControl x:Class="Grove.UserInterface.TurnNumber.View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border>
        <Border.Resources>
            <Style BasedOn="{StaticResource Label}" TargetType="TextBlock">
                <Setter Property="Margin" Value="0,0,5,0" />
            </Style>
        </Border.Resources>        
        <ContentControl Style="{StaticResource Separator}">
            <StackPanel Margin="6,0,4,0" Orientation="Horizontal">
                <TextBlock Text="Turn:" />
                <TextBlock Foreground="#FFD700" Text="{Binding Number}" />
            </StackPanel>
        </ContentControl>        
    </Border>
</UserControl>
